---
description: Start the VIVERSE AI Agent server locally
---

1. Navigate to the `viverse_ai_agent` directory.
2. Ensure you have dependencies installed:
```bash
npm install
```
// turbo
3. Start the server in development mode:
```bash
npm run dev
```

The server is now running on http://localhost:3000.
